#!/usr/bin/env ansible-playbook

- hosts: all
  become: true
  roles:
    - goldbuild

- hosts: openshift-allinone
  become: true
  roles: 
    - repos_openshift
    - docker

- hosts: ansible
  become: true
  roles:
    - repos_ansible
    - docker
    - ansible_setup_bundle
