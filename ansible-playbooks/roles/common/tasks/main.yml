---
- yum: name={{item}} state=present
  with_items:
    - firewalld
    - vim-enhanced
    - elinks
    - ntp
    - git
    - wget
    - unzip 
    - psmisc
    - htop
    - libsemanage-python
- service: name={{item}} state=started enabled=yes
  with_items: 
    - ntpd
